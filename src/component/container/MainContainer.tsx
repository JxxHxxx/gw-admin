

export default function MainContainer({ children }) {

    return <div id="cfd_container_1000" style={{ width: '1000px', maxWidth : '1000px', border: '1px dashed red' }}>
        {children}
    </div>
}